class SourceModel:
	def __init__(self, name: str, weight_path: str):
		self.name: str = name
		self.weight_path: str = weight_path
		self.model: torch_model = None
	
	def create_model(self) -> torch_model:
		""" Creates the model object in memory uninitialized"""
		pass
	
	def load(self):
		""" Loads weights from weight_path into the model"""
		pass
	
	def reset_bn(self):
		"""Resets batch norm statistics for the loaded model"""
		pass



